<a href="/post/{{id}}" class="text-decoration-none">
            <h2 class="text-light">{{title}}</h2>
            </a>
        <p>
            Written by: 
            <strong>{{user.username}}</strong>
            on {{format_date created_at}}
            </p>
        
        <span class="badge bg-primary text-wrap">Comments: {{comments.length}}</span>